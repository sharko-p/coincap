export type HistoricalData = {
    time: number;
    priceUsd: string;
  }
  
  export type CryptoData = {
    id: string;
    symbol: string;
    name: string;
    priceUsd: string;
    marketCapUsd: string;
  }